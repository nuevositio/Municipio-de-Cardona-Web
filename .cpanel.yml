---
deployment:
  tasks:
    - export DEPLOYPATH=/home/adminmlc/cardona
    - /bin/mkdir -p $DEPLOYPATH
    - /bin/cp -R . $DEPLOYPATH
    - cd $DEPLOYPATH && /usr/local/bin/npm install --omit=dev --prefer-offline 2>&1
