node_modules
.env
# dist — incluido para deploy en cPanel (sin acceso SSH)
# dist
uploads
*.zip
# server/dist — incluido para deploy en cPanel
# server/dist
server/tsconfig.tsbuildinfo
server/data/*.sqlite
server/data/*.sqlite-shm
server/data/*.sqlite-wal
server/data/sessions.db
